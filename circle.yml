machine:
  java:
    version: openjdk8
dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "tools,android-25,build-tools-25.0.1,platform-tools,extra-android-m2repository,extra-google-m2repository"